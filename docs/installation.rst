============
Installation
============

At the command line::

    $ easy_install django-admin-views

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-views
    $ pip install django-admin-views
